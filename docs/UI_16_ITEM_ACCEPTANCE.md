# UI and interface revision: 16-item acceptance ledger

Implementation is in progress. A checked item requires code and behavior evidence; existing tests alone do not establish acceptance.

## Live deployment and expression endpoint checkpoint: 2026-09-17

- The backend was restarted with the separate least-privilege local PostgreSQL runtime role. Flyway validated all 30 known migrations, found schema `akis` at version `029` with no pending migration, and `/actuator/health` returned HTTP 200 / `UP`.
- The previously reported live `400` for `POST /api/v1/projects/{projectUuid}/mapping-expressions/compile` was reproduced as an invalid verification URL: it used the numeric internal project key (`1`) where the controller requires the project's UUID. A read-only request using the stored project UUID returned HTTP 200 with the expected `COLUMN` AST and `SOURCE_1.CREATED_AT` reference.
- This closes the stale-listener/404 deployment observation for the expression endpoint. It does not establish persisted interface editing, publication, remote Oracle execution, or all 16 acceptance items.

## Live connection layout checkpoint: 2026-09-17

- The real authenticated browser session opened Connections successfully. Both actual cards showed Record Information with aligned Test Connection button bounds (top 875.84, bottom 909.84 in the inspected viewport). Card/list/table retained their displayed status, dates, counts, endpoint and record-information values. Light and dark card screenshots were visually inspected after theme transitions settled. This is read-only live display evidence, not database mutation or current credential validity evidence.
- Live inspection exposed a remaining legacy rule: `.connections-search` retained horizontal grid columns/auto-flow, so Search connections sat beside its input while other labels sat above. The unused horizontal rule was removed; the catalog's shared field layout now controls all three. Live DOM measurements after hot reload showed all three control tops at 247.80 and label bottoms at 241.80, a consistent 6 px gap.
- Added full-production-route filter regression scenarios with synthetic read-only APIs for EN/TR and light/dark at 1366/768/390 widths. **Four scenarios passed in 10.6 seconds.** They assert label-above-control positioning, aligned desktop fields and no document horizontal overflow. Initial test selectors incorrectly assumed Ant Select was a direct child; selectors were corrected to respect the shared Select wrapper.
- The local backend remains PID 33648, started 01:36:02; frontend remains PID 45152. The variable's existing TEST history still displays the earlier ORACLE_28001 result. No fresh Oracle test or SQL compile was submitted, so the error is historical evidence, not a new credential probe. Backend restart/deployment approval and secure credential renewal remain pending.
- Browser preferences were restored to English/Light and connection Cards; the original variable screen was reopened with Save disabled. No records, test history or runtime values were changed. No restart, migration, commit or push occurred. This checkpoint does not accept all 16 requirements.
- Production build and lint passed after the CSS/test changes; scoped whitespace checks passed. No full frontend unit rerun was made for this CSS-only application change. All verification processes reached terminal results.

## Variable input and KM audit checkpoint: 2026-09-17

- Variable testing no longer substitutes a sample SQL statement when the current query is missing/malformed. The editor displays empty content honestly; the new-definition template still contains its explicit initial query. Empty/whitespace/oversized queries disable testing with a visible explanation. The submitted SQL is exactly the current input, not a fallback.
- Testing requires the selected environment to remain present in the refreshed active environment list. A failed context refresh disables testing until successful retry. Previously selected archived environments cannot continue through a stale enabled button. Backend binding validation remains authoritative.
- Focused variable/editor verification initially passed 17 tests; the final receipt below includes the added editor-empty-value regression. Build and lint passed after application changes. This does not supersede the prior 314-test full suite, which predates these changes.
- Item 6 was traced through KnowledgeModuleRegistry, StagedRuntimePlanResolver, OracleKmRuntime, StagedWorkerOrchestrator and StagedPublishFacade. Current acceptance requirements now supersede outdated pilot limitations in `docs/architecture/KNOWLEDGE_MODULE_ACCEPTANCE.md`. Declared options are pinned/validated; LKM DISTINCT/hint and IKM write mode/hint reach the runtime. Arbitrary new Java/SQL operations are not implied by adding options.
- The scoped backend command passed **83 tests in nine suites**, zero failures/errors/skips, Maven BUILD SUCCESS at 04:41:40. These are unit/mocked JDBC checks, not remote Oracle or production persistence evidence. No backend source was changed.
- No service restart, migration, credentials change, business SQL, commit or push occurred. Live endpoint deployment, successful Oracle scalar/reverse testing and authorized write-mode acceptance remain open; all 16 items are not accepted.
- Final focused frontend receipt: **18 tests in two files passed**, start 04:42:36, duration 8.52 seconds. The KM/variable browser suite passed **6 scenarios in 28.1 seconds**, including empty-query rejection and archived-environment refresh in both themes; its responses are synthetic. Lint passed after all test edits. This checkpoint did not rerun the complete frontend suite. All verification sessions ended normally.

## Multi-source mapping suggestions checkpoint: 2026-09-17

- Inspection found that Suggest Matches selected the first same-named source regardless of ambiguity or type and only considered already-existing mapping rows. Suggestions now cover unmapped target metadata columns, require one same-named source with compatible known type metadata, and preserve existing direct mappings and expressions. Ambiguous names require explicit source selection; no source-order guess is applied.
- Proposals are invalidated by definition changes or metadata refresh. Applying revalidates source/target identities rather than relying on array indexes. Missing captures supply no candidate columns. Explicit application is undoable.
- Adding a join after deleting an earlier join now allocates an unused ID, avoiding duplicate React keys/diagram identities. Existing IDs are preserved.
- Focused verification passed **16 tests in two files**. Production build and lint passed after correcting TypeScript indexed-access guards. Four component regressions cover proposal application, ambiguity feedback, invalidation and delete/re-add join identity; eight helper tests cover type/metadata/existing-expression protections and identity handling.
- Full frontend and authoring browser receipts are recorded below after completion. The first two new browser scenarios had an incorrect Undo expectation: the fixture already contains an ID mapping, which must remain after undoing the suggestion. The assertion was corrected; this was not a production mapping failure.
- No backend change, service restart, migration, credential edit, business SQL, commit or push occurred. These changes do not resolve the pending live endpoint/deployment and Oracle credential checks or establish completion of all 16 requirements.
- Full frontend unit receipt: **314 tests in 92 files passed**, start 04:33:51, duration 241.75 seconds, exit 0. No application source edits overlapped this run. Lint also passed after the browser test correction.
- Final authoring browser rerun: **17 scenarios passed in 2.0 minutes**, exit 0, including the new suggestion/invalidation/Undo journeys in both themes. These use real UI components and intercepted synthetic API responses, not live database persistence. Scoped whitespace checks passed. All launched verification processes reached terminal results.

## Pinned mapping metadata checkpoint: 2026-09-17

- Code inspection found that MappingGrid always displayed the newest capture, even when a source/target reference named an older `schemaSnapshotUuid`. MetadataRepository persists that exact capture in the version's object references, so the editor and published runtime could disagree about available columns and their types.
- Mapping catalog resolution now honors the bound capture for diagram rows, inspector inputs, mapping suggestions and filter/SQL completion catalogs. A missing bound capture does not fall back to the latest one; existing mappings remain intact and a notice explains recovery. Unpinned legacy references still preview the latest capture with an explicit unassigned-version notice rather than silently changing the definition.
- A newer capture is adopted only by explicit catalog-object reassignment. Existing reference cleanup applies, and Undo restores the old capture plus its mappings. Model catalog-change events refresh the available history without automatically changing pins. Project changes clear old metadata, and late responses from the previous project are discarded.
- Focused verification passed **9 tests in three files**. The first attempt failed because the test fixture's `output` element also has the implicit status role; the fixture was corrected to a labeled div. A test-only unsupported Testing Library option was also removed after the build identified it. Production build and lint then passed.
- **15 authoring browser scenarios passed in 1.6 minutes**, including two new physical model-drop journeys in light/dark themes. They show old pinned columns despite newer metadata, apply a mapping, explicitly adopt the new capture, verify removed-column mapping cleanup, and undo back to the old metadata/mapping. The dark screenshot was inspected. API responses are synthetic and no live reverse/import/persistence is claimed. The prior combined 25-scenario receipt predates this change.
- No backend source changed in this checkpoint. No service restart, migration, credential change, business DML, commit or push occurred. Backend restart approval and valid Oracle credentials remain pending. The full frontend unit run is recorded after completion below.
- Final full frontend unit receipt: **302 tests in 91 files passed**, start 04:26:01, duration 195.42 seconds, exit 0. No application-source edits overlapped this run. Scoped diff whitespace checks passed. The full 16-item goal remains open; these results do not supersede the live endpoint/Oracle blockers.

## Mapping consistency and live deployment checkpoint: 2026-09-17

- Direct `alias.column` mapping in the column inspector now uses the same type-family compatibility rule as drag-and-drop. Incompatible direct mappings retain the existing mapping and explain that an explicit SQL conversion is required. Free SQL expressions still go through the compiler; this is design-time guidance, not full static type inference or replacement of runtime validation.
- Catalog column rows, diagram nodes and the inspector now share length/precision formatting. Native types are preserved, temporal precision (including zero) is displayed, and numeric precision takes precedence over driver storage-byte length. No P/L prefixes or placeholder dashes are introduced. Removing a mapping immediately invalidates pending compilation so a delayed response cannot restore the removed mapping.
- Focused regression passed **15 tests in five files**, including incompatible direct mapping, explicit conversion, temporal precision and removal during compilation. Build and lint passed, as did scoped diff whitespace checks. The latest full frontend receipt remains 290 tests from the preceding checkpoint, not a full rerun of these new tests.
- A combined browser run passed **25 scenarios in 2.5 minutes**, covering authoring, KM/variables, production-shell procedure layout, record cards and model-folder editing in both themes. The mapping-expression journeys now assert rejection of an incompatible direct mapping before applying a valid expression. These automated APIs/session responses remain synthetic.
- In the real authenticated Chrome session, the existing 13-column interface opened by double-click. The target TIMESTAMP(6) inspector displayed precision 6; a direct NUMBER-to-TIMESTAMP mapping was rejected without enabling Save. The full actual diagram was visually inspected. No definition was saved, no metadata was imported and no source/target SQL was executed.
- A real design-time `TO_CHAR` compile request returned **404**, unlike the synthetic browser tests. The endpoint exists in the current source, but the local backend listener on 8080 is an AKIS Spring Boot process started at 01:36:02, before later endpoint work. This establishes a live deployment/endpoint gap; current-source tests do not prove that the running service serves the new endpoint. Requested approval for a backend-only restart without migrations or job execution; no restart has occurred. The browser was restored to the original variable with Save disabled.
- Successful remote Oracle execution is still unproven; the previously persisted password-expiry result was viewed, not retried. The full 16-item goal remains active. No migration, credential change, business DML, commit or push occurred.

## Model folder and reverse-discovery checkpoint: 2026-09-17

- Existing Data Stores can now change catalog folder from the detail panel or return to the model root. A dedicated project/model-scoped PATCH changes only folder, version and update timestamp. It preserves object UUID/reference and schema/mapping identities. The service rejects foreign-model objects/folders, archived objects/models, missing folders and invalid versions. SQL repeats scope, active-folder and optimistic-version guards; stale updates return a conflict without an automatic retry.
- The shared Ant folder selector shows full nested paths and excludes other models. Pending requests disable controls; leaving the editor cannot cause a late navigation/result. Success refreshes the Models tree and keeps the selected object open in its new folder. Model/folder editing now uses the same catalog-write permission as the backend, rather than the unrelated connection-management permission.
- Custom RKM discovery was previously presented as active while the request only ran standard JDBC discovery. This fallback is now explicitly disabled for Custom RKM models, with an explanation. This is an interim correctness guard, **not implementation or acceptance of custom RKM execution**. Model edits retain stored reverse options when the selected mode/module has not changed, instead of replacing them with an empty object.
- Full backend unit run passed **628 tests in 109 fresh suites**, zero failures/errors/skips (reports after 04:04:00). Seven new folder-service/controller tests cover scope, permissions, archived records and concurrency. The clean-database catalog integration test was extended for folder round trips and stale versions but was **not executed**; no database was provisioned/migrated for it.
- **31 focused frontend tests in five files passed**. Initial failures were test selector issues; the build then exposed an invalid Testing Library option in the new test. Those were corrected; the model-page pair passed again, and production build/lint passed after all application edits. This checkpoint did not rerun the complete frontend suite; its last full receipt remains 276 tests above/below.
- Two production-route browser scenarios with a synthetic session and intercepted catalog/PATCH APIs passed in **25.2 seconds**. They verify folder moves and return to root, selected-object retention, catalog-write UI permissions, reload against the updated synthetic catalog, and containment at 1366/768/390 widths in both themes. Compact light/dark screenshots were inspected. This does not prove production database persistence or real authorization.
- No actual user catalog object was moved, no services restarted, and no migration, credential edit, business DML, commit or push occurred. Custom RKM, live successful reverse/scalar execution and the broader release acceptance remain open. Audit actor capture also needs wider verification: this folder update follows existing catalog timestamp/version conventions and does not establish that all catalog mutations populate the updating-user field.

## Latest checkpoint: metadata retry and conditional KM steps (2026-09-17)

- Metadata import now rereads the model catalog at every attempt. A registered object whose column snapshot failed is reused on retry, rather than blindly created again. Only completed snapshots clear selection; partial progress remains visible. Leaving the screen prevents subsequent requests, without pretending an already submitted request was cancelled. Seven helper tests and four screen tests cover partial success, uncertain create responses, stale catalog reads, ambiguity and unmount boundaries. This is mocked recovery evidence, not a successful live metadata import.
- AKIS_KM/2 supports `ADIM ... EGER BOOLEAN_OPTION`. The condition must reference a Boolean declared in the same module. Publication uses resolved versioned option values to compile the effective plan; runtime independently recompiles from pinned values and compares exact effective steps. Unknown slots and broken loading/publication dependencies fail before runtime effects. CKM checks may be disabled individually; required LKM/IKM lifecycles cannot disappear. Existing finite operations remain the limit; custom RKM and arbitrary programmable Java/SQL are not implemented by this change.
- The KM editor documents conditional syntax and shows each parsed step's Run Condition. It distinguishes language validation from execution and explains that TRUNCATE requires both the matching write mode and Boolean confirmation. Details and limitations are in `docs/architecture/KM_CONDITIONAL_STEPS.md`.
- Fresh full backend unit run produced **621 tests in 108 suites**, with zero failures, errors or skips (reports after 03:55:00). Focused frontend verification passed **14 tests in three files**. Production build and lint passed. Full frontend results are recorded after completion below. No new browser/live Oracle acceptance is claimed for this checkpoint.
- No service restart, migration, credential edit, business DML, commit or push occurred. Successful remote Oracle execution, custom reverse module behavior, real persistence/publication and the wider 16-item acceptance remain open. Earlier live Oracle credential-expiry evidence was not rechecked this turn.
- Final receipts: **276 frontend tests in 84 files passed** (03:55:15 start, 185.89 seconds, exit 0). Six KM/variable browser scenarios passed in 29.1 seconds, including new conditional-diagnostic checks at 1366/768/390 widths in both themes and existing unsaved variable result/error/history checks. Browser APIs were intercepted with synthetic responses; no business query was submitted. Lint passed again after the browser test was added. No application-source edits overlapped the full unit run. Scoped diff whitespace checks passed.

- [ ] 1. Shared record cards and Record Information / Kayıt Bilgileri heading.
- [ ] 2. Peer card audit and Last Test/action rows align with wrapped content.
- [ ] 3. Compact searchable, resizable Project Objects explorer; double-click/Enter opens, no edit buttons.
- [ ] 4. Remove Sources and Target form; diagram manages objects, aliases and removal.
- [ ] 5. Draggable filter component, free SQL predicate, validation and legacy filter migration.
- [ ] 6. Usable executable LKM/CKM/IKM modules with versioned options.
- [ ] 7. Hide interface Transfer Safety Limits while retaining runtime protections.
- [ ] 8. Clearly separated resizable procedure steps/details; preserve command tab selection.
- [ ] 9. Matching Models explorer, model/folder/datastore double-click, folder assignment and column details.
- [ ] 10. Column inspector maximize/restore/collapse and manual resize.
- [ ] 11. Column dragging does not pan/scroll the canvas; whole-row drop targets.
- [ ] 12. Component-level source/filter/join/target edges instead of per-column edges; execution order agrees.
- [ ] 13. Stable column/type/size/status slots; mapped/unmapped/incompatible states and type icons.
- [ ] 14. Display native database types; keep internal canonical types out of display.
- [ ] 15. Free SQL Source Mapping with alias-dot completion, apply/validation and runtime persistence.
- [ ] 16. Test unsaved variable query in its editor with context/result/duration/error; separate TEST history, no current-value mutation.

Verification must include targeted domain tests, build, updated browser journeys and visual checks in light/dark themes. Destructive source/target SQL is not part of UI verification.

## Evidence checkpoint: 2026-09-17

This is an implementation checkpoint, not acceptance of all 16 requirements.

- Full frontend unit run: 70 files / 221 tests passed before the latest focused changes. The new mapping reference cleanup suite then passed 5 tests; procedure editor/SQL panel suites passed 19 tests after the layout changes. Production build and lint passed.
- `e2e/authoring-workbench.component.spec.ts` uses real components with synthetic catalog API responses. Object heading opens properties; alias editing, maximize/restore, keyboard resize, collapse/reopen, removal and undo passed in the browser. No production definition was saved.
- The same browser fixture verified separate procedure panels in light/dark themes at 1366x900 and 1366x768. It checks visible SQL area, full Apply-button visibility, selected step visibility, and retained Target Command selection. An initially passing but weak editor-height assertion missed clipping; the stronger assertions exposed it and the layout was corrected.
- Procedure step panel now adapts to available height, reserves detail space, and keeps the selected row in its own scroll area. SQL variable help is available in the inline header tooltip and in the expanded editor.
- Replacing/removing a mapping object clears dependent AST column references, filters and joins. Literal text resembling an object reference is preserved. Target removal retains an empty target drop zone.
- Physical `dragTo` tests in Windows/Edge timed out during the native move/up action. This is NOT verified acceptance of item 11. HTML5 event/payload tests are separate component evidence; physical mouse dragging still needs confirmation.
- Five browser component journeys now pass: object properties; HTML5 column payload/drop handling in light/dark; procedure layout in light/dark. Compatible drops map, incompatible drops do not, and the canvas transform stays unchanged during those simulated drops. Native types and mapping state are checked. Inspector restore checks that both source and target rows are fully visible.
- A visual check found that opening/restoring the inspector clipped diagram nodes even though earlier assertions passed. The diagram now refits on viewport resize, with a guard against refitting during a column drag; the browser assertions cover this regression.
- The final focused frontend regression run passed 27 tests across five files (procedure editor, SQL panel, diagram, object assignment and reference cleanup). React Flow now receives the resolved light/dark theme so its zoom controls follow the application theme.

## Known remaining scope (do not treat as complete)

- Items 5/12: draggable filter components, free predicates and component-level flow edges are implemented (see the latest checkpoint). Still require real-session persistence/publication tests and larger multi-source layouts. Physical column, model and palette dragging now have component-level coverage in the checkpoints below; production-shell acceptance remains open. Join conditions are edited in the conditions panel, opened from the Join node.
- Item 15: free SQL scalar expressions, alias-dot completion and staged runtime rendering are implemented. Still require authenticated API/persistence and successful remote Oracle execution. The parser intentionally rejects statements, subqueries, arbitrary packages/functions and unsupported syntax; it is not a complete Oracle SQL grammar. Expression types are checked from JDBC results and typed staging, not fully inferred statically.
- Item 6: versioned KM options exist, but executable LKM/CKM/IKM behavior still requires a complete requirement-by-requirement audit.
- Items 1/2/3/9/13/14: shared presentation/explorer implementations need full-screen, responsive and theme acceptance, not only narrow component tests.
- Item 16: prior live variable test reached Oracle but returned ORACLE_28001 (expired password). The sanitized failure and separate test history were verified; successful remote scalar testing awaits a valid connection credential, updated securely outside chat. No account/password changes were made.
- Production-shell layout, model-object dragging, reverse engineering and end-to-end persistence remain part of final verification. The fixture does not prove these.

## Runtime checkpoint: 2026-09-17

- Existing structured SOURCE filters now run inside each source inline view before joins. GLOBAL filters remain in the final WHERE. Bind parameters follow actual SQL order, including filtered sources traversed in a different order from the filter list. LIKE uses a single-character Oracle escape.
- `StagedQueryGraph` is a metadata-only shared planner used by definition validation and JDBC query rendering. Composite join predicates stay together in ON; reversed LEFT/RIGHT joins preserve direction; extra INNER-cycle predicates are not discarded. Unknown/self edges, disconnected sources, conflicting relation types and ambiguous OUTER cycles are rejected rather than partially executed. Source aliases must be unique, target filter references are rejected, and join/filter lists are bounded.
- An additional runtime defect was found and fixed: staged schema preflight previously checked every mapping against only the first source. Pinned snapshot loading now verifies all source bindings against the same project/publication and builds an immutable object-indexed snapshot map. Initial preflight attests each source on the shared source session; locked target preflight checks all pinned source bodies again before target writes.
- Staged mapping compatibility resolves each column by source object. Reusing one source column for multiple targets is allowed; duplicate target assignments remain rejected. Join/filter columns are checked even when not projected. Null-extended OUTER-join sources cannot silently satisfy non-null target columns. Legacy single-source preflight entry points reject multi-source staged plans.
- Focused regression coverage includes source/global predicate placement, composite/reversed/cyclic joins, malformed graphs before remote SQL, second-source drift, same-name columns from different sources, missing/tampered source snapshots, cross-publication bindings, read-only preflight, and OUTER-join nullability.
- Final backend full unit rerun at 02:09 passed 565 tests in 104 fresh suites, with zero failures, errors or skips. A preliminary aggregate included stale reports and was discarded. This is mocked/unit evidence, not a successful Oracle transfer or database integration test. No business DML, migration, account change, commit or push was performed for this checkpoint.
- At that checkpoint, free expressions/predicates and flow nodes were still open. Their subsequent implementation is recorded below. Full runtime/UI acceptance of all 16 items remains open. The staged multi-source query requires sources on one connection version. These tests do not establish cross-database join support.

## SQL authoring and component-flow checkpoint: 2026-09-17

- Added the bounded `MappingSql` parser/AST validator/renderer and project-scoped `mapping-expressions/compile` endpoint. Aliases resolve to stable source IDs; runtime revalidates all referenced columns against pinned catalogs. SQL values are JDBC binds, never concatenated SQL. Supports scalar functions including TO_CHAR, arithmetic, concatenation, CASE, comparisons, AND/OR/NOT, IN/BETWEEN/LIKE and null checks. LIKE ESCAPE is validated as one character and bound separately.
- Source Mapping accepts free expressions with alias-dot/native-type completion. Apply changes the in-memory mapping; Save remains explicit. Delayed responses after input/context changes are ignored. Unsupported stored expressions no longer crash the inspector or silently delete the original mapping.
- Expressions are carried through schema-4 validation, signed publication resolution, pinned-schema preflight and JDBC SELECT projection. The target staging type and actual JDBC result metadata protect the write boundary. Static expression type inference and successful remote execution are not established by these tests.
- Numeric SQL lexemes now use a string-backed NUMBER AST node so browser JSON processing cannot round large integers/precise decimal literals. Legacy numeric LITERAL nodes remain readable. Tests cover exact JSON round trips and malicious numeric payload rejection.
- Fixed-operator filter rows were replaced with a free SQL editor. Legacy filters display an equivalent condition, retaining string binds and LIKE escape semantics; conversion occurs only after explicit successful Apply. Invalid/cancelled edits preserve the original filter. Scope changes limit both completion and validation to appropriate sources.
- A draggable Filter palette component opens the condition panel when dropped on a source column. Merely opening/dropping does not add a filter. Successful Apply adds it to the diagram; reopen/edit/remove are supported. The same editor serves the conditions panel.
- Diagram edges now represent source → source filters → join → global filters → target, not individual column mappings. Column drag/drop and mapped ticks remain independent. The Join node opens join editing; disconnected sources are marked incomplete. Runtime rejects invalid graphs independently of the drawing.
- Free SOURCE predicates execute inside their own source inline view; GLOBAL predicates execute after joins. Runtime tests cover OUTER-join placement, bind ordering, escaped LIKE, cross-source SOURCE rejection, missing columns, mixed legacy/free fields and immutable predicate copies. Reference cleanup covers predicates as well as projection expressions.
- Nine browser journeys passed in both light/dark variants where applicable: object properties, filter component drop/apply/reopen/remove, SQL completion/apply/reopen, column HTML5 payload/drop and procedure split layout. Screenshots were visually reviewed; unassigned status dots were made neutral and flow edges use the theme primary color. These are synthetic-catalog, real-component tests, not live production persistence.
- Full backend unit run before the final exact-number regression passed 587 tests across 106 fresh suites, zero failures/errors/skips. Production frontend build and lint passed. Focused filter/flow frontend suites passed 9 tests; the unreadable-expression inspector suite passed 4 tests. Final broader reruns are tracked separately below.
- No business DML, migration, account/password change, commit or push was performed in this checkpoint. Running-service deployment of the new endpoint and real-session validation are still pending.
- Final backend rerun after the exact-number change passed 588 tests in 106 fresh suites (zero failures/errors/skips). The exact-number frontend regression and related filter/reference suites passed 11 tests; build and lint passed again. An earlier full frontend run overlapped source changes and failed on the newly added NUMBER case while using an older imported module; it is not accepted as a passing receipt. A fresh full rerun was started after edits stopped.
- Item 6 audit finding at that checkpoint: new KM defaults began with the legacy metadata-only editor. The next checkpoint below records its correction; existing legacy definitions remain preserved rather than silently converted.

## KM and variable editor checkpoint: 2026-09-17

- New knowledge modules now start with an editable AKIS_KM/2 IKM template. LKM/CKM/IKM template changes require confirmation and replace only the unsaved content. Existing legacy modules retain their content and explicit conversion path. New mappings default to schema 4 instead of opening the legacy compatibility path.
- Option display names/descriptions are retained separately from executable declarations and appear in mapping option forms. ENUM allowed values are editable. Quoted tokens preserve multi-word hints and literal `YOK`; backend parsing rejects malformed quotes, invalid defaults and malformed ENUM lists. Option-key edits retain focus and temporarily incomplete form rows. Unparsed manually authored declarations are preserved when other form options change, rather than silently discarded.
- Variable test sessions are isolated by project and definition: navigating while a request is pending cannot inject the old variable's result, leave the new screen busy or retain the previous environment selection. Existing query/context-change warnings remain.
- Visual review exposed legacy toast positioning/flex CSS that clipped long Ant notifications. Removed those legacy layout rules and added browser geometry assertions for multiline error text. Variable results use a separate full-width result row so long errors no longer squeeze the type/duration/context fields. Light/dark screenshots were inspected after correction.
- Focused frontend regression: 17 tests passed across four files. Focused backend regression: 28 tests passed across five suites. The subsequent full backend unit run passed 590 tests in 106 fresh suites, zero failures/errors/skips (reports written after 02:51:35 local).
- Final browser component run: 13 passed, including the existing nine authoring journeys and four new KM/variable journeys. KM templates/options were checked at 1366, 768 and 390 px, in light/dark themes. Variable unsaved test requests, success/failure history, context, stale-result warning and toast containment were checked in both themes. API responses are synthetic; this does not establish successful live Oracle execution or production-shell persistence.
- The legacy new-module browser test was updated to the executable editor contract; that authenticated production-shell journey has not yet been rerun. Build and lint passed. No service restart, database migration, business DML, credential change, commit or push was performed.
- Remaining item 6 concerns include a full runtime acceptance audit and integer-option fidelity: the current browser parser coerces INTEGER defaults to JavaScript Number and the backend registry accepts any Number, so precision/range/integrality need explicit treatment. Generic user-defined option keys are pinned but do not independently introduce execution behavior; do not describe this as arbitrary programmable KM support.
- Final frontend full unit run after these changes: 77 files / 246 tests passed (02:49:46 start, 223.09 seconds, exit 0). This run completed without further application-source edits. The final browser rerun passed all 13 scenarios; an earlier combined attempt was not accepted because its toast assertion used the wrong DOM descendant and a fixture edit triggered hot reload during a test.

## Integer fidelity, explorer isolation and pointer checkpoint: 2026-09-17

- INTEGER options now preserve signed 64-bit values as decimal strings instead of rounding through JavaScript Number. The shared input retains invalid raw text with an error and does not silently clamp or truncate on blur. Backend registry validation rejects fractional, nonfinite and out-of-range numbers, while preserving wire representations for publication comparisons. Legacy metadata defaults receive the same bounds check.
- Runtime option guards now validate built-in semantics even if a module changes its declaration type: hints remain validated strings, IKM-only options cannot be attached to other roles, TRUNCATE requires a Boolean and merge keys require validated identifiers. This is not arbitrary programmable KM support; the remaining runtime audit is unchanged.
- Mapping KM option forms now show versioned defaults when an override is absent. Clearing an integer override removes the key, restoring the actual runtime default instead of persisting null. Exact large-integer default/edit/clear behavior is covered.
- Models explorer sessions are keyed by project and guarded by request generation. Responses from an old project or from before an explicit catalog refresh cannot resurrect obsolete entries. Model navigation/reverse/drag production test locators now follow the new tree, but authenticated production journeys have not been run.
- The previous physical-column test timeout was reproduced. In-canvas column dragging now uses pointer capture instead of the OS HTML5 drag loop. A second defect was exposed: controlled React Flow nodes lost measured dimensions on hover updates, temporarily hiding drop targets. Dimensions are now retained through node changes.
- Real Playwright mouse movements now verify whole-row compatible drops, incompatible target feedback/rejection, Escape cancellation, no accidental inspector opening, unchanged canvas transform/page scroll, and inspector readback of the new mapping in both themes. HTML5 payload compatibility tests remain distinct. This establishes component mouse behavior, not production model/palette dragging or persistence.
- Full backend unit rerun starting 03:07:41 passed **596 tests in 107 fresh suites**, zero failures/errors/skips. The frontend full run starting 03:07:28 passed **253 tests in 79 files** before the subsequent audit-presentation consolidation. Build and lint passed after that consolidation; focused audit/connection tests passed 8 tests in three files.
- The combined authoring/KM/variable browser run passed 15 scenarios before the new record-catalog checks. Record card/list/table checks subsequently passed in both themes at 1366, 768 and 390 px, including aligned audit/description/footer tracks, edit-button-only opening, identical values/status and contained actions. Visual review then removed audit-field ellipsis so long authors and dates wrap fully. A final combined rerun is pending below.
- Record audit values now share one formatter across connection cards/tables and generic audited grids. Loading/error/missing states previously differed in casing and semantics; they now remain identical without representing a fetch error as missing data.
- Authenticated UI test credentials are not defined in this process. No service restart, migration, credential edit, business DML, commit or push was performed. Successful remote Oracle testing and the full production-shell acceptance remain open.
- Final verification after the audit wrapping change: **255 frontend tests across 80 files passed** (03:12:51 start, 184.26 seconds, exit 0); **17 browser component scenarios passed** (1.6 minutes); production build and lint passed. Final light desktop and dark mobile record screenshots were inspected: long author/date values are fully wrapped, peer footers remain aligned, and buttons stay within card boundaries. Earlier record-browser attempts failed on hidden native radio inputs and a textContent/innerText comparison in the test; these were corrected without force-clicking or relaxing layout assertions. API data in browser fixtures is synthetic; this result is not release-wide/live-database acceptance.

## Explorer, publication defaults and variable failure checkpoint: 2026-09-17

- Added an explorer workbench with the real ProjectSidebarTree and MappingGrid. Physical mouse tests verify single-click selection versus double-click/Enter opening, folder and view routes, Reverse Engineer action routing, absence of edit buttons, and Models root expansion without navigation. A model datastore can be dragged from the tree to the diagram, its assigned logical schema read back, and the change undone. A separate physical palette drop opens a column-scoped filter editor without creating a filter before Apply. This does not prove that reverse engineering itself succeeds against a remote database.
- Visually inspected the explorer screenshot: hierarchy, folder/view/table icons and native-type mapping rows are legible with no page-width overflow at 1600x1000. This is one fixture viewport, not all production-shell responsive states.
- Fixed publication DDL risk metadata: `nonReversibleDdl` now uses resolved IKM options, including versioned defaults, instead of only raw mapping overrides. Regression tests cover TRUNCATE_LOAD defaults, APPEND overrides and the resulting physical-plan hash. Initial new-test failures were caused by the test calling the wrong planner method and omitting snapshot UUIDs; corrected fixtures now satisfy the actual publication contract.
- Added mocked writer regressions for APPEND count accounting, key-only MERGE SQL, unmapped/missing merge keys, no repeated TRUNCATE for already-recorded publications, and UNKNOWN rather than ROLLED_BACK after uncertain TRUNCATE or a subsequent insert failure. Updated the writer comment so it no longer describes TRUNCATE_LOAD as an atomic/no-DDL path. These tests are not proof of Oracle DDL locking, fencing or remote recovery behavior; that runtime audit remains open.
- Added variable service tests for empty results, multiple rows, multiple columns and null values. Each verifies failed TEST history, resource closure and no writes to runtime-value history or definition drafts. The editor's seven existing tests also passed again.
- Fresh full backend run starting **03:24:33** passed **608 tests in 108 suites**, with zero failures, errors or skips; only reports written after that start were counted. The browser run passed **19 component scenarios** (1.9 minutes). Production frontend build and lint passed. The full frontend unit suite was not rerun this checkpoint; the latest full receipt remains 255 tests above, with seven focused variable tests passed this turn.
- No service restart, migration, remote business DML, credential change, commit or push occurred. Valid authenticated production testing, successful remote Oracle execution, persistence/publication and the complete 16-item acceptance audit remain outstanding. No item is marked complete solely from these component/unit results.

## Runtime option contract and real-shell layout checkpoint: 2026-09-17

- Runtime now revalidates pinned KM options using the same type, role, required/default and built-in semantic checks as publication. AKIS_KM/2 executable SECENEK declarations remain authoritative, not display metadata. Missing resolved defaults are rejected instead of silently introducing new execution behavior. Integer overflow, duplicate declarations, undeclared options, invalid hints, missing MERGE keys and unconfirmed TRUNCATE have regression coverage, including physically rehashed invalid plans.
- New physical pins include the versioned option schema for AKIS_KM/1. Optionless legacy plans remain accepted. Existing configurable legacy publications that omit their declaration schema must be republished; runtime does not guess their option types. No publication was rewritten or migrated. This is validation of the existing bounded KM operations, not arbitrary Java/SQL programmability or proof of remote DDL recovery safety.
- Fixed explorer ancestor expansion: a selected object several folders deep now reveals its full rendered parent path. Tests include nested folders, recovered orphan/cycle hierarchy and Enter navigation.
- Added `shell-authoring.component.spec.ts` against production App/AppShell/DefinitionsWorkspace routes, with synthetic session and intercepted read-only APIs. It verifies a multi-step procedure, persistent Target Command selection, visible Apply/SQL area, separate step/details panels and sidebar resize/reset. Desktop checks cover 1366x900 and 1366x768; compact checks cover widths 768 and 390 in both themes. These tests do not establish real authentication, saves or remote execution.
- A visual review caught a defect that the initial small-screen test missed: the details grid expanded to min-content width and horizontally scrolled fields/footer off-screen. Constrained the grid track and SQL panel minimum width. Strengthened tests to check internal detail widths and SQL editor bounds, not just document overflow or button visibility. Narrow screens retain vertical detail scrolling and SQL's own horizontal scroll for long statements.
- Initial shell tests also exposed an overly broad test interception pattern that intercepted a JavaScript module path containing `/api/`; interception now matches only versioned API URLs. No application authentication behavior was changed to make the fixture pass.
- Full backend run starting **03:29:41** passed **615 tests in 108 fresh suites**, zero failures/errors/skips. Focused explorer tests passed **12 tests in two files**. Production build and lint passed after the layout fix. Final combined browser and frontend unit receipts follow when complete.
- E2E username/password environment variables remain unset (only presence was checked, no values read). No service restart, migration, credential edit, business DML, commit or push occurred. The full goal remains open.
- Final receipts: **257 frontend tests / 80 files passed** (03:31:10 start, 222.23 seconds); **21 browser scenarios passed** (2.1 minutes), including the stronger small-screen internal-overflow checks; build and lint passed after the CSS correction. Desktop dark and compact dark screenshots were visually inspected. The unit run overlaps the CSS-only correction but no application TypeScript changes; the browser/build runs used the corrected CSS. All browser API data and the shell session are synthetic.

## Live model and variable verification checkpoint: 2026-09-17

- The existing authenticated Chrome session was accessible. A real variable query test returned `ORACLE_28001` (expired Oracle password), with type/context/duration and a separate TEST-history entry. Reloading the page retained that history entry; the definition Save action remained disabled and runtime value history remained empty. This verifies the persisted failure path, not a successful Oracle query. Credential renewal was requested without requesting a password in chat.
- Model double-click navigation and datastore double-click column details were exercised against the real catalog. Native Oracle types appeared in the column table. No model, datastore, definition, connection credential or business record was edited through the UI.
- The live model page exposed duplicate Data Store List headings and an incorrect no-data message when records existed but none was selected. Fixed these, distinguished a missing selected object, and made the read-only detail action say View rather than Edit. Audited grids now honor explicit collection titles; datastore names identify cards rather than the first Type cell. Discovery controls respect the discovery permission.
- Model detail sessions are isolated by project/model, with request generation protection. New tests cover stale route results, retry after a failed next-model load, stale column snapshots, card double-click and table keyboard opening. Temporal column precision now displays from `timePrecision`, alongside native types, without internal canonical-type labels or P/L prefixes.
- Metadata discovery sessions are isolated by project/model/object/folder. Environment and scope cannot change during discovery/import; a new discovery or scope change clears old results, selection and preview. Late discovery responses cannot fill a different model screen. Discovery errors and import feedback use the shared toast. The model-name back link returns to that model, not the catalog root.
- A bounded live metadata-only discovery failed with the service's generic discovery error. It is not counted as reverse-engineering success, and its exact remote cause was not established from that generic message. The pending UI context controls were visibly disabled and became usable again after failure. No metadata import was submitted.
- Light table and dark card views were inspected in the real model page. A shared-card CSS specificity conflict placed grid-only action footers at the left; fixed their right alignment without changing connection cards' two-sided timestamp/action footers. The corrected dark view was inspected. Original light theme and table view were restored, and the browser was returned to the variable definition.
- Ten new model/detail/discovery unit tests passed; 14 focused model-tree/model-detail/connection tests also passed. Initial new-test failures were corrected selector/fixture issues and an incorrect toast import, all resolved before the passing runs. Full frontend results and the final build/lint receipt follow below. Backend code was unchanged this checkpoint.
- Remaining work includes successful remote Oracle testing, real metadata import and definition persistence/publication, broader production responsive verification, and the KM runtime acceptance audit. Custom RKM execution and partial metadata-import recovery also need an explicit audit; a saved RKM label is not evidence of execution. No item is accepted solely because its unit tests pass. No migration, service restart, credential change, business DML, commit or push was performed.
- Code inspection confirmed that the current metadata discovery request passes connection/version/physical schema and table scope, but not a model RKM definition or its options. The Custom RKM model setting is therefore not evidence of a functioning custom reverse module. This remains an implementation gap, not a validated feature.
- Final verification: **267 frontend tests in 83 files passed** (03:44:37 start, 170.88 seconds, exit 0). Production build and lint passed after the footer CSS correction; scoped `git diff --check` passed. The full unit run overlapped only that CSS correction, not TypeScript edits. The 21 browser automation receipt above predates this checkpoint; this turn adds actual Chrome model/card/column navigation observations and new unit tests, not a new automated 21-scenario run.

## Model folders and record attribution checkpoint: 2026-09-17

- Added a scoped, optimistic-versioned Data Store folder move endpoint and an in-place folder editor. Only active folders in the same model/project are accepted; root placement is supported. The selected object remains visible after moving, and explorer refresh follows the changed catalog. Model/folder actions now use catalog write permission rather than connection-management permission. Added repository integration coverage, but did not run the clean-database integration test or provision a database.
- Model edits preserve existing reverse options when the reverse mode/module is unchanged. Custom RKM discovery is explicitly unavailable rather than silently running standard JDBC discovery. This guard is not an implementation of custom RKM execution; that gap remains open.
- Data Store and model-folder creation now return exact Location headers, allowing the existing HTTP audit filter to identify the newly created record rather than its parent model. Record attribution recognizes nested model/Data Store paths and v2 connection paths. Unhandled request exceptions and non-2xx responses are no longer recorded as successful writes. A newer event without a captured principal no longer inherits an unrelated previous editor's name.
- Successful record writes invalidate only the matching project's record-kind attribution. Cards and grids refresh their Record Information without a page reload. Connection probes, variable tests and SQL validation do not count as record edits. Tests cover stale responses, project changes, failures and event-listener cleanup.
- Latest full frontend unit run: **290 tests / 88 files passed**, start 04:11:06, duration 210.80 seconds, exit 0. Fresh backend reports written after 04:12:00 contain **635 tests / 110 suites, zero failures, errors or skips**. The full backend process's terminal output was lost during context transfer; these counts are report evidence, not a recovered process-exit receipt. Build and lint were rerun and passed; `git diff --check` passed.
- The latest four-scenario model-folder/record-catalog browser run has a passed Playwright last-run result. Its tests use production model routes with synthetic authentication/catalog/PATCH responses, and record fixtures. They cover both themes, folder/root moves, immediate updated-author refresh, reload against the synthetic catalog and 1366/768/390 px containment. The compact dark folder screenshot was inspected. These are not real persisted database writes or successful Oracle acceptance.
- Record attribution SQL currently has structural/mocked JDBC regression coverage, not a fresh real-PostgreSQL execution receipt. HTTP audit capture remains best-effort, not transactionally atomic with the business write. Historic records without captured identity remain honestly unknown. Successful live Oracle queries, real metadata import, custom RKM execution and full production persistence/publication acceptance remain open. No service restart, migration, credential change, business DML, commit or push occurred in this checkpoint.
